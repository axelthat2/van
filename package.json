{
  "name": "van",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:sourcemap": "VITE_SOURCEMAP=true vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sentry/cli": "^1.73.2",
    "vite": "^2.8.0"
  },
  "dependencies": {
    "@sentry/browser": "^6.18.2",
    "@sentry/tracing": "^6.18.2"
  }
}
